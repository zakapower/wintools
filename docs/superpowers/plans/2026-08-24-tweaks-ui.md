# Tweaks UI Redesign Implementation Plan

> **For agentic workers:** Implement task-by-task. Steps use checkbox (`- [ ]`) syntax.

**Goal:** Turn Tweaks into a Windows Settings–style vertical list with official names + gray hints.

**Architecture:** Keep tweak state/`patch` in `Generator.tsx`. Add `choice__body` / `choice__hint` markup; restyle `.choices--tweaks` to one column. No XML changes.

**Tech Stack:** Next.js, React, existing Generator CSS.

## Global Constraints

- Official Windows names in titles; short bilingual hints via `t()`.
- Approach A layout (hint right on desktop, under title on mobile).
- No new config keys; no unattend behavior changes.
- Apps chip grid unchanged.

---

### Task 1: CSS list layout + hint styles

**Files:** `src/components/Generator.css`

- [x] Change `.choices--tweaks` to single full-width column.
- [x] Add `.choice__body` / `.choice__hint` desktop + mobile rules.
- [x] Verify tweaks rows don't fight existing `.choice` card styles.

### Task 2: Markup + copy for all tweak rows

**Files:** `src/components/Generator.tsx`

- [x] Wrap each tweak title+hint in `choice__body`.
- [x] Add RU/EN hints matching current XML behavior.
- [x] Style Express privacy radios the same way.

### Task 3: Smoke-check

- [x] Confirm localhost Tweaks section renders as list; mobile stacks hints.
- [x] Run existing `buildUnattendXml` tests (unchanged behavior).
